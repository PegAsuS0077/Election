# Set to your FastAPI backend URL when the backend is running
# Leave blank to use mock simulation
VITE_API_URL=http://localhost:8000

# Set true only during AdSense review to disable non-Google monetization behavior.
# Default/production-safe for full ads is false (or unset).
VITE_ADSENSE_REVIEW_MODE=false
